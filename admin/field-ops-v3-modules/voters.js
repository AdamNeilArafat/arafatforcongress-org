export function filterRecords(records,q,filters){const s=String(q||'').toLowerCase();return records.filter(r=>{const hay=[r.full_name,r.address,r.phone,r.email,r.zip].join(' ').toLowerCase();if(s&&!hay.includes(s))return false; if(filters.district&&filters.district!=='all'&&r.district!==filters.district)return false; if(filters.status&&filters.status!=='all'&&r.status!==filters.status)return false; if(filters.priority&&filters.priority!=='all'&&r.priority!==filters.priority)return false; if(filters.assigned&&filters.assigned!=='all'&&r.assigned_volunteer!==filters.assigned)return false; return true;});}
export function sortRecords(records,key,dir='asc'){const sign=dir==='asc'?1:-1;return [...records].sort((a,b)=>String(a[key]??'').localeCompare(String(b[key]??''))*sign);}
export function toCsv(rows){const h=Object.keys(rows[0]||{});return [h.join(','),...rows.map(r=>h.map(k=>`"${String(r[k]??'').replace(/"/g,'""')}"`).join(','))].join('\n');}
