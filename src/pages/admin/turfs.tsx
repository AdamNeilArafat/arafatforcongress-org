import React from 'react';
import { MapShell } from '../../components/Map/MapShell';

export default function AdminTurfsPage() {
  return <MapShell title="Admin Turfs" points={[]} pending={0} blocked={0} failed={0} />;
}
